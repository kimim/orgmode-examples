clean:
	-rm -f *.vrb *.tex *.log *.aux *.log *.out *.html *.pyg *.java *.class
	-rm -f *.bcf *.fdb_latexmk *.fls *.nav *.run.xml *.snm *.xdv
	-rm -rf _minted*
	-rm -rf svg-inkscape
	-rm -rf *.bbl *.blg *.toc
